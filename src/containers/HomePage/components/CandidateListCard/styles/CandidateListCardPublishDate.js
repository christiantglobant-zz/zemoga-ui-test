import styled from "styled-components";

const CandidateListCardPublishDate = styled.time.attrs(() => ({
  className: "CandidateListCardPublishDate"
}))`
  font-size: 0.8rem;
  font-weight: bold;
`;

export default CandidateListCardPublishDate;
