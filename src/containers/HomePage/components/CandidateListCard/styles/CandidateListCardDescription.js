import styled from "styled-components";

const CandidateListCardDescription = styled.div.attrs(() => ({
  className: "CandidateListCardDescription"
}))`
  margin: 1rem 0;
  max-width: 25rem;
  font-weight: 300;
`;

export default CandidateListCardDescription;
