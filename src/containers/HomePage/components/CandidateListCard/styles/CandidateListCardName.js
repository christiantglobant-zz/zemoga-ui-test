import styled from "styled-components";

const CandidateListCardName = styled.div.attrs(() => ({
  className: "CandidateListCardName"
}))`
  font-size: 3rem;
  max-width: 25rem;
  margin-bottom: 0.3rem;
`;

export default CandidateListCardName;
