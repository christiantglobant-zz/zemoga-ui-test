import styled from "styled-components";

const PrincipalBannerTitle = styled.div.attrs(() => ({
  className: "PrincipalBannerTitle"
}))`
  font-size: 2.3rem;
  line-height: 2rem;
  font-weight: bold;
`;

export default PrincipalBannerTitle;
