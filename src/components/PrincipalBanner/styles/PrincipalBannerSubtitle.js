import styled from "styled-components";

const PrincipalBannerSubtitle = styled.div.attrs(() => ({
  className: "PrincipalBannerSubtitle"
}))`
  font-weight: 100;
  font-size: 1.4rem;
`;

export default PrincipalBannerSubtitle;
